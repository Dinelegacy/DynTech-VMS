# Environment & OS
.env
.env.local
.env*.local
.DS_Store

# Next.js Build Output & Cache (matches both root and frontend subfolder)
**/.next
.next
out
build

# Dependencies (matches both root and frontend subfolder)
**/node_modules
node_modules

# Logs
*.log